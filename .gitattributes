# Blok `run: |` di GitHub Actions dieksekusi oleh bash. Kalau file-nya ber-CRLF
# (mudah terjadi kalau diedit di Windows), baris jadi "set -e\r" dan langkahnya gagal.
*.yml text eol=lf
*.yaml text eol=lf
*.sh text eol=lf
