import { PageHeader } from '../../components/common/PageHeader';
import { Card } from '../../components/ui/Card';
import { DataTable } from '../../components/common/DataTable';
import { approvalStatusLabel } from '../../utils/crossEnrollment';
export const StatusKrsLintasPage = () => <div className="space-y-4"><PageHeader title="Status KRS Lintas Program Studi" subtitle="Pantau persetujuan program studi penyelenggara" breadcrumbs={[{ label: 'KRS' }, { label: 'Status Lintas Prodi' }]} /><Card title="Riwayat Pengajuan"><DataTable resource="krs-lintas-prodi" rowKey={(row) => row.id} searchPlaceholder="Cari mata kuliah..." columns={[{ key: 'matakuliah_id', header: 'Mata Kuliah', render: (row) => `${row.penawaran?.matakuliah?.kode_matakuliah || ''} ${row.penawaran?.matakuliah?.nama_resmi || ''}` }, { key: 'prodi_host_id', header: 'Penyelenggara', render: (row) => row.penawaran?.semesterProdi?.programStudi?.nama_resmi || '—' }, { key: 'status_home', header: 'Prodi Asal', render: (row) => <span className="badge badge-ghost badge-sm">{approvalStatusLabel(row.status_home)}</span> }, { key: 'status_host', header: 'Prodi Penyelenggara', render: (row) => <span className="badge badge-ghost badge-sm">{approvalStatusLabel(row.status_host)}</span> }, { key: 'status', header: 'Status Akhir', render: (row) => <span className="badge badge-primary badge-sm">{approvalStatusLabel(row.status)}</span> }]} /></Card></div>;
