import { DataTable } from '../common/DataTable';

export const CoursePickerTable = ({ courses, selected, quotas, defaultQuota, onToggle, onToggleAll, onQuotaChange }) => {
  const allSelected = courses.length > 0 && courses.every((row) => selected.includes(row.id));
  return <DataTable data={courses} tableKey="course_pick_" rowKey={(row) => row.id} searchableFields={['kode_matakuliah', 'nama_resmi']} searchPlaceholder="Cari mata kuliah program studi..." emptyText="Pilih semester dan program studi untuk memuat mata kuliah." columns={[{ header: <input type="checkbox" className="checkbox checkbox-sm" checked={allSelected} onChange={(event) => onToggleAll(event.target.checked)} aria-label="Pilih semua mata kuliah" />, render: (row) => <input type="checkbox" className="checkbox checkbox-sm" checked={selected.includes(row.id)} onChange={() => onToggle(row.id)} aria-label={`Pilih ${row.nama_resmi}`} /> }, { key: 'kode_matakuliah', header: 'Kode', sortable: true }, { key: 'nama_resmi', header: 'Mata Kuliah', sortable: true }, { key: 'jumlah_sks_kurikulum', header: 'SKS', sortable: true }, { key: 'semester_kurikulum', header: 'Semester Kurikulum', sortable: true }, { key: 'kuota_lintas_prodi', header: 'Kuota Lintas', render: (row) => <input type="number" min="0" className="input input-sm w-24" value={quotas[row.id] ?? defaultQuota} disabled={!selected.includes(row.id)} onChange={(event) => onQuotaChange(row.id, event.target.value)} aria-label={`Kuota ${row.nama_resmi}`} /> }]} />;
};
