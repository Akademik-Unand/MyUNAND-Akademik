'use strict';const v=require('../../../src/validations/krs/cross-enrollment.validation');describe('cross enrollment validation',()=>{test('requires offering and class UUID',()=>{expect(v.enroll.validate({}).error).toBeTruthy();expect(v.enroll.validate({penawaran_matakuliah_id:'bad',kelas_id:'bad'}).error).toBeTruthy();});test('requires reason on rejection',()=>{expect(v.decision.validate({approved:false}).error).toBeTruthy();expect(v.decision.validate({approved:false,reason:'Tidak memenuhi syarat'}).error).toBeFalsy();});});
